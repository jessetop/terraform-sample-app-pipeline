# terraform.tfvars
# Replace studentXX with your assigned student ID

student_id = "studentXX"

# After Part A (initial deployment), run `terraform output` and record this value:
#
#   state_bucket_name = "<your-bucket-name-will-appear-here>"
#
# Then for Part B (migration), uncomment the backend block in providers.tf
# and replace the placeholder bucket value with your actual bucket name.
